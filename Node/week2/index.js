const fs = require("fs").promises;
const path = require("path");

async function main() {
  try {
    // const data = await fs.readFile("fajlli.txt");
    // console.log(data.toString());
    // fs.writeFile("tekst.txt", "kete file e kemi krijuar e nodejs");
    // fs.unlink("tekst.txt");
  } catch (err) {
    throw err;
  }
}

main();
