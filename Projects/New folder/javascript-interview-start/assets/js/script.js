let movies = Document.queryselector(".p-0 movieGernes");


let movielist ='';

allMovies.array.forEach(element => {
    movielist += '<option value="${item.gerne}">${item.gerne}</option>';
});

movies.gernes.innerHTML = allMmovielistmovies;
